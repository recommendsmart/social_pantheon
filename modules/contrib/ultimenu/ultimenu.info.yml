name: Ultimenu
type: module
description: Provides Ultimenu blocks, and regions based on available menus.
core: 8.x
core_version_requirement: ^8 || ^9
configure: ultimenu.settings
dependencies:
  - drupal:block

# Add a fake version so local drush and Git checkouts do not fail dependencies.
# version: 8.x-2.x

# Information added by Drupal.org packaging script on 2020-01-15
version: '8.x-2.2'
project: 'ultimenu'
datestamp: 1579094587
