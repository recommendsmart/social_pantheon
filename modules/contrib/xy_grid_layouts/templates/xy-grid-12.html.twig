{% extends '@xy_grid_layouts/xy-grid-wrapper.html.twig' %}

{% block grid %}
    {% if content.main %}
        <{{ settings.wrappers.main }} class="cell {{settings.breakpoint}}-12">
        {{ content.main }}
        </{{ settings.wrappers.main }}>
    {% endif %}
{% endblock %}
